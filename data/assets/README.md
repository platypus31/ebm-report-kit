# data/assets — 自備固定素材

放**跨報告重複使用**的固定圖片素材，例如：

- 6S 證據金字塔圖
- OCEBM 2011 證據等級表
- 院內教學用的流程圖

這些圖多半有版權，**不隨本 repo 附帶**，請自行放入（檔名自訂，在 `content.json` 的 `figure` 頁用 `path` 指過去）。

沒有這些圖也能跑完整個流程：簡報引擎遇到缺圖會畫佔位框，不會中斷。
另一個做法是用 `table`／`textcard` 頁型把內容重製成可編輯的原生頁面，效果通常比貼圖更好。
