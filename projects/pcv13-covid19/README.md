# 範例專案：PCV13 與 COVID-19（/ebm-from-paper 完整示範）

本專案是 `/ebm-from-paper` 反向流程的完整實跑範例（PMID 33693636，預防型問題、CASP Cohort 12 題）。
包含 01–05 各步驟產出與 `06_slides/content.json`（v3 可編輯版式）。

⚠️ 配圖產物（論文 PDF、佐證裁圖、檢索截圖、匯出 pptx）**不隨 repo 提供**——跑 skill 時會自動下載論文與生成；
content.json 內的 figure path 為相對路徑，缺圖時引擎會畫佔位框，不影響流程驗證。
